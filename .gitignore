# General explanatory header
# -----------------------------------------------------------------------------
# Standard .gitignore file for Web Frontend Development Projects
#
# Covers common files/directories generated by:
# - Package managers (npm, yarn, pnpm)
# - Build tools (Webpack, Vite, Parcel, Rollup, etc.)
# - IDEs/Editors (VS Code, WebStorm, Sublime Text, etc.)
# - Operating Systems (macOS, Windows, Linux)
# - Logging and testing output
# -----------------------------------------------------------------------------
# Dependency directories
# -----------------------------------------------------------------------------
node_modules/

# Build output directories
# -----------------------------------------------------------------------------
dist/
build/
out/
public/
.parcel-cache/
.svelte-kit/
.nuxt/
.next/
.vercel/

# Compiled files (if not in a build directory)
# Be careful with broad rules; prefer specific build directories above.
# *.js
# *.css
# *.html

# Source Maps (Often large, sometimes ignored, sometimes kept for debugging)
# -----------------------------------------------------------------------------
*.js.map
*.css.map

# Log files
# -----------------------------------------------------------------------------
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment variables and secrets
# Commit example/template files, but NEVER actual secrets.
# -----------------------------------------------------------------------------
.env
.env.*
!.env.example
!.env.template
# Add other specific secret files if any (e.g., firebase.json if private)

# IDE / Editor specific files
# -----------------------------------------------------------------------------
.idea/           # JetBrains IDEs (WebStorm, IntelliJ, etc.)
.vscode/         # VS Code (Consider keeping .vscode/settings.json if shared team settings are desired)
*.sublime-project
*.sublime-workspace
.project         # Eclipse
.classpath       # Eclipse
.settings/       # Eclipse
*.tmproj         # TextMate

# OS generated files
# -----------------------------------------------------------------------------
.DS_Store        # macOS
.DS_Store?       # macOS
._* # macOS
.Spotlight-V100  # macOS
.Trashes         # macOS

# Temporary files
# -----------------------------------------------------------------------------
*.swp            # Vim swap files
*~               # Backup files
*.tmp

# Testing and Coverage reports
# -----------------------------------------------------------------------------
coverage/
.nyc_output/
junit.xml
*.html # If test reports generate HTML files in the root or specific non-build folders

# Package Manager cache / lockfiles (specific versions/types)
# NOTE: Generally, commit package-lock.json, yarn.lock, or pnpm-lock.yaml!
# These are examples of things sometimes ignored.
# -----------------------------------------------------------------------------
.npm/
.yarn/cache
.yarn/install-state.gz
.yarn/unplugged
.pnp.* # Yarn PnP files

# Other common tool caches/outputs
# -----------------------------------------------------------------------------
.eslintcache
.stylelintcache
.node_repl_history

# END of standard .gitignore